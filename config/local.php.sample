<?php
/**
 * 設置先ごとの設定（雛形）
 * ============================================================
 * このファイルを config/local.php という名前にコピーして使います。
 * config/local.php は Git 管理外（.gitignore 済み）なので、
 * default.php を触らずに土台の設定を書き換えられ、Git 更新でも競合しません。
 *
 * 重なる順: default.php → local.php → <プロジェクトID>.php
 * 書いた項目だけが上書きされ、ここに書かなかった項目は default.php のままです。
 *
 * ※ 「SHOW_IN_SELECT」だけは既定プロジェクトの表示可否にしか効きません
 *    （各 config/<ID>.php には継承されません）。
 * ============================================================
 */

return [
    // 何も指定せずに開いたときのプロジェクト（保存フォルダ名にもなる）
    // 'PROJECT_ID' => 'PRJ-0001',

    // 既定プロジェクトを切り替えプルダウンに載せない
    // 'SHOW_IN_SELECT' => false,

    // 切り替えプルダウンに載せる・送信を受け付けるプロジェクトを絞る（空なら制限なし）
    // 'SERVER' => [
    //     'ALLOWED_PROJECT_IDS' => ['PRJ-0001', 'PRJ-0002'],
    // ],

    // 備考をファイル名に付けない（画像モード）
    // 'FILENAME' => [
    //     'APPEND_NOTE' => false,
    // ],
];
